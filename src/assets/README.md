# assets — Static Assets

Images, icons, and other static files imported by the app or bundled by Vite.

Most in-game iconography is drawn from `react-icons` or as inline SVG in the
components themselves rather than shipped as files here, so this folder stays
small.
