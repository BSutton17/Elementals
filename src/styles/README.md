# styles — Global Styles

App-wide styling and design tokens.

- `index.css` — global base styles (imported once in `main.tsx`)
- Future: shared CSS variables / theme tokens

Component- or screen-scoped styles live next to their component/page rather than
here.
