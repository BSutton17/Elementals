# components — Reusable UI Components

Small, reusable React presentation components (buttons, panels, HUD widgets,
status indicators). Animated with Motion where appropriate.

- Presentational and composable; avoid embedding networking or game logic here.
- Screen-specific compositions live in `pages/`; shared pieces live here.
